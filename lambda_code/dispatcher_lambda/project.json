{
  "name": "dispatcher-lambda",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "lambda_code/dispatcher_lambda",
  "projectType": "application",
  "tags": ["layer:lambda", "type:lambda", "scope:dispatcher"],
  "targets": {
    "build": {
      "executor": "@nx/esbuild:esbuild",
      "dependsOn": ["^build"],
      "outputs": ["{options.outputPath}"],
      "options": {
        "tsConfig": "tsconfig.node-paths.json",
        "bundle": true,
        "platform": "node",
        "target": "node20",
        "format": ["esm"],
        "main": "lambda_code/dispatcher_lambda/index.js",
        "outputPath": "dist/lambda_code/dispatcher_lambda",
        "outputFileName": "main.js",
        "external": ["@aws-sdk/*"],
        "esbuildOptions": {
          "logLevel": "warning"
        }
      }
    }
  }
}
