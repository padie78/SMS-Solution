/**
 * Port: outbound queue dispatch.
 *
 * @typedef {Object} InvoiceQueue
 * @property {(params: { bucket: string, key: string, orgId: string, sk: string, requestId: string }) => Promise<void>} enqueueInvoice
 */

export {};

