/**
 * Port: resolve organization id for a given S3 object.
 *
 * @typedef {Object} OrgResolver
 * @property {(params: { bucket: string, key: string, requestId: string }) => Promise<import('@sms/common').SmsId>} resolveOrgId
 */

export {};

