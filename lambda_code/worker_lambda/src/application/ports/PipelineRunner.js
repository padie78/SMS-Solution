/**
 * Port: invoice processing pipeline runner.
 *
 * @typedef {Object} PipelineRunner
 * @property {(params: import('@sms/common').InvoiceWorkerPipelineInput & { sk: import('@sms/common').InvoiceSk }) => Promise<void>} run
 */

export {};

