{
  "name": "signer-lambda",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "lambda_code/signer_lambda/src",
  "projectType": "application",
  "tags": ["type:lambda", "scope:signer"],
  "targets": {
    "build": {
      "executor": "@nx/esbuild:esbuild",
      "dependsOn": ["^build"],
      "outputs": ["{options.outputPath}"],
      "options": {
        "tsConfig": "lambda_code/signer_lambda/tsconfig.json",
        "bundle": true,
        "platform": "node",
        "target": "node20",
        "format": ["esm"],
        "main": "lambda_code/signer_lambda/src/index.js",
        "outputPath": "dist/lambda_code/signer_lambda",
        "outputFileName": "main.js",
        "external": ["@aws-sdk/*"],
        "esbuildOptions": {
          "logLevel": "warning"
        }
      }
    }
  }
}
