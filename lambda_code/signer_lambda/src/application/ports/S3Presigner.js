/**
 * Port: S3 presigning (outbound dependency).
 *
 * @typedef {Object} S3Presigner
 * @property {(params: { bucket: string, key: string, contentType: string, expiresInSeconds: number }) => Promise<string>} presignPutObject
 */

export {};

