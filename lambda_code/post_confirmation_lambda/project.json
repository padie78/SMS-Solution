{
  "name": "post-confirmation-lambda",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "lambda_code/post_confirmation_lambda/src",
  "projectType": "application",
  "tags": ["type:lambda", "scope:auth"],
  "targets": {
    "build": {
      "executor": "@nx/esbuild:esbuild",
      "outputs": ["{options.outputPath}"],
      "options": {
        "tsConfig": "lambda_code/post_confirmation_lambda/tsconfig.json",
        "bundle": true,
        "platform": "node",
        "target": "node20",
        "format": ["esm"],
        "main": "lambda_code/post_confirmation_lambda/src/index.js",
        "outputPath": "dist/lambda_code/post_confirmation_lambda",
        "outputFileName": "main.js",
        "external": ["@aws-sdk/*"],
        "esbuildOptions": {
          "logLevel": "warning"
        }
      }
    }
  }
}
