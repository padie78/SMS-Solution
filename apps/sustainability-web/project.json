{
  "name": "sustainability-web",
  "root": "apps/sustainability-web",
  "projectType": "application",
  "sourceRoot": "apps/sustainability-web/src",
  "tags": ["scope:web", "type:app"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "../../node_modules/.bin/ng build sustainability-web --configuration=production",
        "cwd": "apps/sustainability-web"
      }
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "../../node_modules/.bin/ng serve sustainability-web",
        "cwd": "apps/sustainability-web"
      }
    }
  }
}