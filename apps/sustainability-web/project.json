{
  "name": "sustainability-web",
  "root": "apps/sustainability-web",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/dist/apps/sustainability-web"],
      "options": {
        "command": "npx ng build --configuration=production",
        "cwd": "." 
      }
    }
  }
}