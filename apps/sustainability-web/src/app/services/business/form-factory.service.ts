import { Injectable } from '@angular/core';

/**
 * Central factory for reactive invoice/hierarchy forms.
 * Upload Step 1 no longer collects hierarchy — assignment happens in validation (Step 2).
 */
@Injectable({ providedIn: 'root' })
export class FormFactoryService {}
