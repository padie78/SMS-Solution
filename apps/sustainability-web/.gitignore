# Hereda reglas del .gitignore raíz del monorepo.
# Entradas específicas de esta app (por si se trabaja solo en esta carpeta):

.angular/
dist/
out-tsc/
tmp/
node_modules/

# Amplify / config generada
src/assets/config.json
aws-exports.js
awsconfiguration.json

# PDF worker (postinstall)
src/assets/pdfjs/pdf.worker.min.mjs
