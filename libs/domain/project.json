{
  "name": "domain",
  "root": "libs/domain",
  "projectType": "library",
  "sourceRoot": "libs/domain/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{projectRoot}/dist"],
      "options": {
        "command": "npm run build",
        "cwd": "libs/domain"
      }
    }
  },
  "tags": ["scope:domain", "type:domain-model"]
}
